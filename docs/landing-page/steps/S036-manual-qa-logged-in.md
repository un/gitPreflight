# S036 Manual QA (Logged In)

Goal: validate marketing UX while logged in.

## Result (user-verified)

- `/` CTA shows `Dashboard`
- `/sign-in` redirects to `/dashboard` (no sign-in flash)
- `/dashboard` loads; `/device` still works
